---
'@gpuix/native': minor
'@gpuix/react': minor
---

Add GPU-owned shimmer and fixed-cell terminal elements, extend native motion targets and SVG rotation, provide direct binary terminal frame staging, and keep embedded macOS frame polling nonblocking.

Fix primary click delivery in embedded windows and avoid ending text selection when no drag was active.
